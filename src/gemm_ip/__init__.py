"""
gemm-ip-gen — GEMM IP package generator for the Catapult HLS backend.

Generates blackbox packages for the tensor-slice INT8 GEMM hardblock,
including RTL wrappers, C API models, hls4ml-native adapters, and
backend-specific build scripts.
"""

__version__ = "0.2.0"
